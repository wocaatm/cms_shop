const input = ['email', 'url', 'number', 'tel', 'text', 'password', 'tel']

const textarea = ['textarea']

const date = ['date']

const time = ['time']

const checkbox = ['checkbox']

const radio = ['radio']

const select = ['select']

export {
    input,
    textarea,
    date,
    time,
    checkbox,
    radio,
    select
}